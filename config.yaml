df_id: df-hotel-benchmark-tracker
df_name: Hotel-Benchmark-Tracker
welle_introduced: welle-47
type: foundation-df
crux-mk: true
k0_touch: false
q0_touch: false
phronesis_required: false

# K11 Cascade-Containment
cascade_isolation: hard
failure_blast_radius: 1

# K12 Distillation-Resistenz
output_feeds_into_training: false
provenance_required_in_output: true
non_llm_validation_layer: true

# K13 Independent-Ground-Truth
external_anchor_type: drive_filesystem
external_anchor_validation_frequency: daily
pre_action_domain_check: true
pre_action_backup_check: true
pre_action_blast_radius_check: true

# K14 Human-Override-Decay
override_complexity: single_command
override_documentation_freshness_days: 30
martin_review_cadence: weekly
bus_factor_handover_drilled: false

# K15 Entropy-Budget
entropy_added_loc_estimate: 600
entropy_added_concepts:
  - BenchmarkEngine
  - AlertEngine
  - IQROutlierDetector
entropy_justified_by_rho: 0.05
entropy_subtractive_potential: true

# K16 Concurrent-Spawn-Mutex
concurrent_spawn_protection: both
lock_dir: /tmp/df-hotel-benchmark-tracker.lock
lock_stale_age_h: 6
engine_pgrep_check: true
lock_release_on: EXIT_INT_TERM

# Lose-Coupling LC1-LC5
lose_coupling:
  LC1_graceful_degradation:
    degradation_modes:
      - full
      - degraded_no_real_pms
      - sandbox_mock
    degradation_test_required: true
  LC2_direct_mode:
    direct_mode_available: true
    direct_mode_capability: 0.7
    direct_mode_trigger: "pms unreachable >30s"
  LC3_circuit_breaker:
    circuit_breaker_timeout_s: 30
    circuit_breaker_open_threshold: 3
    circuit_breaker_half_open_test_interval_s: 300
  LC4_failure_isolation:
    state_externalization: true
    idempotent_operations: true
    dependency_dlq_separate: true
  LC5_health_check:
    health_check_dependencies: []
    health_check_degraded_score: 0.7
    health_check_endpoint_or_file: state/health.json

# ENV-Var-Gated-Real-Integration-Default
env_vars:
  DF_BENCHMARK_REAL_PMS_ENABLED: false
  PHRONESIS_TICKET: ""

# Outlier-Thresholds
outlier:
  iqr_multiplier_outlier: 1.5
  iqr_multiplier_severe: 3.0

# Mock-Hotels (KPI-Snapshots)
mock_hotels:
  - tenant_id: hildesheim
    name: HeyLou Hildesheim
    region: DE
    adr: 145.0
    occupancy: 0.78
  - tenant_id: cape-coral
    name: HeyLou Cape Coral
    region: US
    adr: 189.0
    occupancy: 0.82
  - tenant_id: munich
    name: HeyLou Munich
    region: DE
    adr: 220.0
    occupancy: 0.91
  - tenant_id: mock-4
    name: HeyLou Mock-4
    region: DE
    adr: 130.0
    occupancy: 0.65
  - tenant_id: mock-5
    name: HeyLou Mock-5
    region: AT
    adr: 410.0  # Outlier (high)
    occupancy: 0.95

# Scheduling
schedule:
  type: launchagent
  cadence: hourly
  RunAtLoad: true
  StartInterval: 3600
